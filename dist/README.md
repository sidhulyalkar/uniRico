# Deployable artifacts

- `uniRico-v0.14.0-wavedash.zip` is the dedicated Wavedash upload artifact. It contains exactly one top-level `index.html` with the Wavedash readiness handshake inlined after game initialization.

The official js13k competition artifact remains separate so platform-specific host code does not alter the competition build.
